/**
 * Mock-mode demo credentials. Surfaced on the login page when MOCK_DATA=true.
 * Any non-empty email/password works in mock mode — these are only the values
 * the hint suggests, so there's something concrete to type.
 */
export const MOCK_LOGIN_EMAIL = 'sec@lambunao.gov.ph';
export const MOCK_LOGIN_PASSWORD = 'demo';
